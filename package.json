{
  "name": "deckzero",
  "version": "0.1.0",
  "private": true,
  "description": "Opinionated design system primitives for Reveal.js decks.",
  "scripts": {
    "sync-demo-assets": "node ./scripts/sync-demo-assets.mjs",
    "compile": "npm run sync-demo-assets && node ./scripts/compile.mjs",
    "test": "node ./tests/deckzero.test.mjs && node ./tests/compile.test.mjs"
  }
}
